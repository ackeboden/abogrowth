# Netlify-bygge för TanStack Start via nitro.
# NITRO_PRESET=netlify får nitro att skriva serverfunktionen till
# .netlify/functions-internal och statiska filer till dist.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NITRO_PRESET = "netlify"
  NODE_VERSION = "22"
